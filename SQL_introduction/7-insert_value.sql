-- This script inserts a new row into the `first_table` in the `hbtn_0c_0` database in MySQL server.
-- It is assumed that the database `hbtn_0c_0` and the table `first_table` already exist.
INSERT INTO first_table (id, name) VALUES (89, 'Best School');

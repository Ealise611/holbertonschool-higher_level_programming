-- this script prints the description of the `first_table` table in the hbtn_0c_0 database in MySQL server.
-- It is assumed that the database `hbtn_0c_0` and the table `first_table` already exist.
SHOW CREATE TABLE first_table;
